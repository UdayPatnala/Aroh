diff --git a/packages/asdk/src/store/index.ts b/packages/asdk/src/store/index.ts
--- a/packages/asdk/src/store/index.ts
+++ b/packages/asdk/src/store/index.ts
@@ -35,5 +35,5 @@
   // Actions
   login: (email: string, password?: string) => Promise<void>;
   register: (email: string, displayName: string, password?: string) => Promise<void>;
-  logout: () => void;
+  logout: (notify?: boolean) => void;
   upgradeMembership: (level: MembershipLevel, cost: number) => Promise<void>;
@@ -126,11 +126,17 @@
-  logout: () => {
+  logout: (notify = true) => {
+    if (notify && get().isAuthenticated) {
+      if (typeof window !== "undefined") {
+        window.localStorage.setItem("aroh_logout_event", Date.now().toString());
+      }
+    }
     set({
       user: null,
       profile: null,
       wallet: null,
       token: null,
       isAuthenticated: false,
       transactions: [],
       notifications: []
     });
   },
